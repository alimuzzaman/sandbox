# Transcript `01a0068a-ee37-7260-9aff-888a5fd36c89`

Source: `Free up remote server space`
Rollout file: `rollout-2026-08-15T23-49-23-01a0068a-ee37-7260-9aff-888a5fd36c89.jsonl`
Evidence role: remote-only targeting cross-check

## Findings sourced here

### ATO-006 — Remote-only intent guard (P2)

The user explicitly constrained the work to the Sandbox remote, but the agent
initially inspected local state and had to be corrected. Explicit `--remote`
exists, but multi-step workflows do not carry a fail-closed remote-only policy.
Add a target receipt or `--remote-only` execution context, show the resolved target
in every receipt, and refuse implicit local probing without changing the explicit
`--local` override.

### ATO-021 — Remote resource preflight (P1/P2)

The same remote-storage/operator evidence included partial inventory and
measurement-unavailable states where cleanup must remain fail-closed. Luna Max's
source review found that workspace mutations use an ownership/runtime-revision
preflight, but `reclaim_service(remote)` builds `RemoteResourceAdapter` directly
and checks only `provisioned` before `/resources` mutations. Apply the same
remote-service receipt to resource observe, lease, release, TTL, reap, and
revalidation so a stale or misattributed service cannot receive cleanup work.
See [canonical finding](../findings.md#ato-021--apply-remote-service-preflight-to-resource-reclamation).
