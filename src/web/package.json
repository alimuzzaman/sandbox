{
  "name": "sandbox-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "TypeScript source for the ./sb web dashboard. Built to a single vendored IIFE bundle (config/sandbox-web.js) that `sb` inlines into the page. Running ./sb web needs no node — only rebuilding does.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
